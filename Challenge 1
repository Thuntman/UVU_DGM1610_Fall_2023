using System;

namespace MyApplication
{
  class Program
  {
    static void Main(string[] args)
    {
      string name = "Challenge 1";
      Console.WriteLine(name);
      {
      bool isCodingDifficult = true;
      Console.WriteLine(isCodingDifficult);
      {
      int x = 5;
      Console.WriteLine(x);
      }
      }
      }
      }
      }
